<h1>Friend Requests</h1>
<%  @friend_requests.each do |request| %>
    <% if request.friend == current_user && request.confirmed == false %>
        <%= request.user.full_name %> sent you a friend request<br>
        <button><%= link_to  "Accept", friendship_path(request), method: :put %></button>
        <button><%= link_to  "Ignore", friendship_path(request), method: :delete %></button><br>
    <% end %>
<% end %>